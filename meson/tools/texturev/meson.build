texturev_root = '../../../bgfx/tools/texturev/'

texturev_exe = executable('texturev', [
  texturev_root + 'texturev.cpp',
  ],
  dependencies: [
    bx_dep,
    bimg_dep,
    bgfx_dep,
    examples_common_dep
  ]
)